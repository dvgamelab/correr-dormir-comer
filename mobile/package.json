{
  "name": "correr-dormir-comer-app",
  "version": "1.0.0",
  "private": true,
  "description": "App Android de Correr·Dormir·Comer (Capacitor)",
  "scripts": {
    "prepare-web": "node prepare-web.mjs",
    "sync": "npm run prepare-web && npx cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.1.2",
    "@capacitor/browser": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/filesystem": "^7.0.0",
    "@capacitor/share": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
